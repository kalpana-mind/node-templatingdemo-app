<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Random</title>
</head>
<body>

  <h1>Your random number is: <%= num %></h1>
  <% if(num %2 === 0){ %>
    <h2>The number is even</h2>
        <% }else{ %>
    <h2>The number is odd</h2>
    <% } %>
    <h3>The number is <%= num%2 === 0 ? 'even ' : 'odd ' %>number</h3>
  
</body>
</html>